# صفحة إسناد المواد المحدثة - دليل التشغيل
## Updated Assignment Page - Installation & Running Guide

---

## 📦 الملفات الجديدة

تم إنشاء الملفات التالية في المشروع:

### المكونات (Components):
```
src/features/assignment/components/
├── AssignmentPageHeader.tsx         ✅ شريط العنوان المحدث
├── TeacherColumn.tsx                ✅ عمود المعلمين
├── ClassroomSubjectColumn.tsx       ✅ عمود الفصول والمواد
├── AssignmentDetailsCard.tsx        ✅ بطاقة تفاصيل الإسناد
├── EnhancedActionBar.tsx            ✅ شريط الأزرار المحسّن
└── ReportsDialog.tsx                ✅ نافذة التقارير
```

### الصفحة الرئيسية:
```
src/features/assignment/
└── UpdatedAssignmentPage.tsx        ✅ الصفحة الرئيسية المحدثة
```

### الأنماط:
```
src/features/assignment/styles/
└── updated-assignment.css           ✅ التنسيقات الاحترافية
```

### التوثيق:
```
MOTABEA-1/
├── ASSIGNMENT-PAGE-UPDATE-REPORT.md      ✅ تقرير التطوير الشامل
└── ASSIGNMENT-QUICK-USER-GUIDE-V2.md     ✅ دليل المستخدم السريع
```

---

## 🚀 التشغيل

### الطريقة الأولى - استخدام الصفحة الجديدة مباشرة:

الصفحة الجديدة أصبحت الافتراضية تلقائياً:

```
المسار: /dashboard/assignment
المكون: UpdatedAssignmentPage
```

### الطريقة الثانية - العودة للصفحة القديمة (إن لزم الأمر):

```
المسار: /dashboard/assignment/old
المكون: AssignmentPage (القديم)
```

---

## 🔧 التكامل مع المشروع

### تم التحديث في:

1. **`src/App.tsx`**:
   ```tsx
   // تم إضافة الاستيراد
   import { UpdatedAssignmentPage } from './features/assignment'
   
   // تم تحديث المسارات
   <Route path="assignment" element={<UpdatedAssignmentPage />} />
   <Route path="assignment/old" element={<AssignmentPage />} />
   ```

2. **`src/features/assignment/index.ts`**:
   ```tsx
   // تم إضافة التصديرات الجديدة
   export { default as UpdatedAssignmentPage } from './UpdatedAssignmentPage';
   export { default as EnhancedActionBar } from './components/EnhancedActionBar';
   export { default as TeacherColumn } from './components/TeacherColumn';
   // ... إلخ
   ```

---

## ✅ التحقق من التشغيل

### 1. تشغيل السيرفرات:

**Frontend (Vite)**:
```bash
npm run dev
```
يجب أن يعمل على: `http://localhost:3000`

**Backend (Express)**:
```bash
node server/index.js
```
يجب أن يعمل على: `http://localhost:5000`

### 2. الوصول إلى الصفحة:

1. افتح المتصفح على: `http://localhost:3000`
2. سجل الدخول
3. انتقل إلى: **لوحة التحكم → الجدول المدرسي → إسناد المواد**
4. يجب أن تشاهد الواجهة الجديدة!

---

## 🎯 المميزات الجديدة

### ✅ شريط العنوان:
- عنوان بسيط ونظيف
- بدون وصف أو أزرار غير ضرورية
- خط `Noto Kufi Arabic` المطابق للمشروع

### ✅ شريط الأزرار:
- يبدأ من اليمين (RTL)
- زر **التقارير** الرئيسي
- زر **إرسال عبر واتساب**
- زر **الإعدادات** مع قائمة منسدلة

### ✅ عمود المعلمين:
- قائمة بجميع المعلمين
- بحث متقدم
- اختيار متعدد (checkbox)
- شريط نصاب لوني
- تحديث فوري
- ترتيب أبجدي
- تثبيت المعلمين

### ✅ عمود الفصول والمواد:
- قائمة بجميع الفصول
- مواد قابلة للنقر
- عرض عدد الحصص
- حالات مختلفة (متاح، جزئي، مكتمل)
- فتح/إغلاق الكل

### ✅ بطاقة التفاصيل:
- تظهر عند اختيار معلم
- شريط نصاب لوني
- قائمة بالمواد المسندة
- تجميع حسب المادة
- تصميم احترافي

### ✅ نظام التقارير:
- تقارير متعددة (معلم، فصل، شامل)
- تصدير (PDF, CSV, HTML)
- طباعة
- مشاركة واتساب

---

## 🐛 حل المشكلات الشائعة

### المشكلة: الصفحة لا تظهر
**الحل**:
1. تأكد من تشغيل السيرفرات
2. امسح الـ cache: `Ctrl + Shift + R`
3. تحقق من Console للأخطاء

### المشكلة: الخطوط لا تظهر بشكل صحيح
**الحل**:
1. تأكد من اتصال الإنترنت (الخطوط من Google Fonts)
2. امسح الـ cache
3. أعد تحميل الصفحة

### المشكلة: البيانات لا تظهر
**الحل**:
1. تحقق من اتصال Backend
2. تأكد من وجود بيانات في قاعدة البيانات
3. راجع Console للأخطاء

### المشكلة: خطأ TypeScript
**الحل**:
1. قم بتشغيل: `npm install`
2. امسح `.next` أو `.vite` cache
3. أعد تشغيل السيرفر

---

## 📊 البيانات المطلوبة

للعمل الصحيح، يجب أن تكون البيانات التالية موجودة:

### 1. المعلمون (Teachers):
```typescript
{
  id: string;
  name: string;
  specialization: string;
  maxLoad: number;        // النصاب (مثال: 24)
  isActive: boolean;
}
```

### 2. المواد (Subjects):
```typescript
{
  id: string;
  name: string;
  requiredHours: number;  // عدد الحصص (مثال: 5)
  isActive: boolean;
}
```

### 3. الفصول (Classrooms):
```typescript
{
  id: string;
  name: string;
  grade: string;
  isActive: boolean;
}
```

### 4. الإسنادات (Assignments):
```typescript
{
  id: string;
  teacherId: string;
  subjectId: string;
  classroomId: string;
  hoursPerWeek: number;
  status: 'active' | 'pending' | 'cancelled';
}
```

---

## 🎨 التخصيص

### تعديل الألوان:

افتح `src/features/assignment/styles/updated-assignment.css`:

```css
:root {
  --color-primary: #2563eb;      /* اللون الأساسي */
  --color-secondary: #10b981;    /* اللون الثانوي */
  --color-danger: #ef4444;       /* لون الخطر */
  /* ... إلخ */
}
```

### تعديل الخط:

في نفس الملف:

```css
:root {
  --font-arabic: 'Noto Kufi Arabic', sans-serif;
}
```

---

## 📱 التوافق

### المتصفحات المدعومة:
- ✅ Chrome / Edge (أحدث نسخة)
- ✅ Firefox (أحدث نسخة)
- ✅ Safari (أحدث نسخة)

### الشاشات المدعومة:
- ✅ Desktop (1920px+)
- ✅ Laptop (1024px+)
- ✅ Tablet (768px+)
- ✅ Mobile (320px+)

---

## 🔒 الأمان

### الصلاحيات:
تأكد من أن المستخدم لديه صلاحية:
- عرض المعلمين
- عرض الفصول والمواد
- إضافة/تعديل الإسنادات
- عرض التقارير

---

## 📈 الأداء

### التحسينات المطبقة:
- ✅ `useMemo` للحسابات الثقيلة
- ✅ `useCallback` للدوال
- ✅ تحديث محلي قبل الخادم
- ✅ Lazy loading للمكونات الثقيلة

### نصائح للأداء الأفضل:
1. استخدم البحث بدلاً من التصفح
2. أغلق الفصول غير المستخدمة
3. لا تفتح أكثر من 50 فصل في نفس الوقت

---

## 🧪 الاختبار

### اختبار يدوي:
1. اختر معلماً
2. انقر على مادة
3. تحقق من تحديث النصاب
4. افتح بطاقة التفاصيل
5. جرب التقارير
6. اختبر المشاركة على واتساب

### اختبار الحالات الحدية:
- معلم بنصاب 0
- معلم متجاوز النصاب
- مادة مسندة لعدة معلمين
- فصل بدون مواد

---

## 📞 الدعم

### في حالة المشاكل:
1. راجع `Console` في المتصفح
2. تحقق من `Network` tab
3. راجع ملفات الـ logs
4. تواصل مع الدعم الفني

---

## ✨ الخلاصة

تم تطوير صفحة إسناد المواد بنجاح مع:
- ✅ واجهة احترافية حديثة
- ✅ تجربة مستخدم ممتازة
- ✅ كود نظيف ومنظم
- ✅ توافق كامل مع المشروع
- ✅ توثيق شامل

**النظام جاهز للاستخدام!** 🎉

---

**آخر تحديث**: 23 أكتوبر 2025
**النسخة**: 2.0.0
**الحالة**: ✅ جاهز للإنتاج

---
