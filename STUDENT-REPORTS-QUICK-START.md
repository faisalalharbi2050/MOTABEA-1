# 🚀 دليل التشغيل السريع - نظام الكشوف

## ✅ المتطلبات

قبل البدء، تأكد من:
- ✅ Node.js مثبت
- ✅ المشروع محمّل على جهازك
- ✅ تم تثبيت الحزم (`npm install`)

---

## 🎯 خطوات التشغيل

### 1. تشغيل الخادم الخلفي (Backend)

افتح Terminal الأول:

```bash
cd C:\Users\fasoo\OneDrive\Desktop\MOTABEA
node server/index.js
```

يجب أن ترى:
```
🚀 MOTABEA Server running on port 5001
📱 Frontend URL: http://localhost:3000
...
```

### 2. تشغيل الخادم الأمامي (Frontend)

افتح Terminal ثانٍ:

```bash
cd C:\Users\fasoo\OneDrive\Desktop\MOTABEA
npm run dev
```

يجب أن ترى:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

### 3. فتح المتصفح

افتح المتصفح وانتقل إلى:
```
http://localhost:3000
```

### 4. تسجيل الدخول

استخدم الحساب الافتراضي:
- **اسم المستخدم**: `admin`
- **كلمة المرور**: `admin123`

### 5. الوصول إلى الكشوف

بعد تسجيل الدخول:
1. انظر إلى القائمة الجانبية اليمنى
2. ابحث عن قسم "**كشوف**" (تحت "نماذج شؤون الطلاب")
3. اضغط على "كشوف"
4. ستفتح صفحة الكشوف الرئيسية

---

## 🎨 جرب الميزة!

### إنشاء كشف جديد

1. اضغط زر "**كشف جديد**" (في الأعلى يمين الصفحة)
2. املأ البيانات:
   - **اسم الكشف**: "كشف اختبار تجريبي"
   - **الفصل**: اختر أي فصل
   - **الشعبة**: اختر أي شعبة
   - **المادة**: اختر أي مادة
3. أضف عمود:
   - اضغط "إضافة عمود"
   - اسم العمود: "اختبار 1"
   - فعّل "يحتوي على درجة"
   - الدرجة العظمى: 10
4. أضف عمود آخر:
   - اسم العمود: "اختبار 2"
   - فعّل "يحتوي على درجة"
   - الدرجة العظمى: 10
5. شاهد المعاينة على اليسار
6. اضغط "**حفظ الكشف**"

### عرض الكشف

1. سيتم نقلك تلقائياً إلى صفحة الكشف
2. ستجد أسماء الطلاب مملوءة تلقائياً
3. جرب:
   - زر "تعبئة البيانات" - لإدخال الدرجات
   - زر "طباعة" - للطباعة
   - زر "تصدير" - (قريباً)

---

## 🔍 استكشاف الأخطاء

### المشكلة: الخادم الخلفي لا يعمل

**الأعراض:**
```
Error: listen EADDRINUSE: address already in use :::5001
```

**الحل:**
```bash
# Windows PowerShell
Get-Process -Id (Get-NetTCPConnection -LocalPort 5001).OwningProcess | Stop-Process -Force

# ثم أعد تشغيل الخادم
node server/index.js
```

### المشكلة: الخادم الأمامي لا يعمل

**الأعراض:**
```
Error: Port 3000 is already in use
```

**الحل:**
```bash
# Windows PowerShell
Get-Process -Id (Get-NetTCPConnection -LocalPort 3000).OwningProcess | Stop-Process -Force

# ثم أعد تشغيل
npm run dev
```

### المشكلة: لا تظهر الكشوف

**الحلول:**
1. تأكد من تسجيل الدخول
2. افتح Console في المتصفح (F12)
3. تحقق من وجود أخطاء
4. تأكد من أن الخادم الخلفي يعمل

### المشكلة: لا يحفظ البيانات

**الحلول:**
1. تحقق من Console (F12)
2. تحقق من Network tab
3. تأكد من أن الـ token صالح
4. جرب تسجيل الخروج والدخول مرة أخرى

---

## 📝 ملاحظات مهمة

### البيانات مؤقتة
⚠️ **تنبيه**: البيانات الحالية مخزنة في الذاكرة فقط.
- عند إعادة تشغيل الخادم، ستُفقد البيانات
- للحفظ الدائم، يجب ربط النظام بقاعدة بيانات

### الطلاب التجريبيون
ℹ️ النظام يستخدم طلاب تجريبيين حالياً.
- أسماء الطلاب: طالب 1، طالب 2، إلخ
- للحصول على الطلاب الفعليين، ربط بقاعدة البيانات مطلوب

---

## 🎓 الخطوات التالية

بعد التجربة الناجحة:

1. **اقرأ التوثيق**:
   - `STUDENT-REPORTS-README.md` - البدء السريع
   - `STUDENT-REPORTS-GUIDE.md` - دليل شامل
   - `STUDENT-REPORTS-DEVELOPER-GUIDE.md` - للمطورين

2. **جرب المميزات**:
   - إنشاء كشوف مختلفة
   - تعبئة البيانات
   - الطباعة
   - البحث والتصفية

3. **التخصيص** (اختياري):
   - ربط بقاعدة بيانات حقيقية
   - إضافة مميزات إضافية
   - تخصيص التصميم

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من قسم "استكشاف الأخطاء" أعلاه
2. راجع ملفات التوثيق
3. تحقق من Console للأخطاء
4. اتصل بفريق الدعم

---

## ✨ نصائح

### للحصول على أفضل تجربة:

1. **استخدم متصفح حديث**:
   - Chrome (موصى به)
   - Firefox
   - Edge
   - Safari

2. **دقة الشاشة**:
   - الحد الأدنى: 1280x720
   - الموصى به: 1920x1080

3. **الطباعة**:
   - استخدم وضع الطباعة في المتصفح
   - اختر "Portrait" للتنسيق الأفضل
   - احفظ كـ PDF إذا لزم الأمر

---

## 🎉 استمتع!

الآن أنت جاهز لاستخدام نظام الكشوف!

**الميزة كاملة وجاهزة للاستخدام الفوري.** 🚀

---

**تاريخ**: 12 أكتوبر 2025  
**نظام متابع - MOTABEA**  
**الإصدار**: 1.0.0
