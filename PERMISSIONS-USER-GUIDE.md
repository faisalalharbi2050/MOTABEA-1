# دليل المستخدم السريع - نظام الصلاحيات المحدّث

## 🎯 نظرة عامة

تم تحديث نظام الصلاحيات ليكون أبسط وأسهل في الاستخدام، مع التركيز على تجربة ممتازة على الأجهزة المحمولة.

---

## 📱 الصفحة الرئيسية (إدارة الصلاحيات)

### المسار:
```
/dashboard/permissions
```

### المكونات:
1. **رأس الصفحة**
   - عنوان "إدارة الصلاحيات"
   - زر "إضافة مستخدم جديد"

2. **جدول المستخدمين**
   - الاسم ورقم الجوال
   - معلومات الدخول (اسم المستخدم + رمز سريع)
   - الصفة (مدير/وكيل/آخر)
   - الحالة (نشط/غير نشط)
   - آخر دخول
   - الإجراءات (3 أزرار)

---

## ➕ إضافة مستخدم جديد

### الخطوات:

1. **الانتقال للنموذج**
   - اضغط على زر "إضافة مستخدم جديد" الأزرق

2. **ملء البيانات الأساسية**
   ```
   ✅ الاسم الكامل (مطلوب)
   ✅ رقم الجوال (مطلوب - يجب أن يبدأ بـ 05)
   ⚪ البريد الإلكتروني (اختياري)
   ✅ الصفة (اختر من القائمة):
      - مدير المدرسة
      - وكيل المدرسة
      - آخر
   ```

3. **معلومات الدخول**
   ```
   ⚪ اسم المستخدم (اختياري - يمكن تسجيله لاحقاً)
   ⚪ كلمة المرور (اختياري - يمكن تعيينها لاحقاً)
   ✅ رمز الدخول السريع (يتم توليده تلقائياً)
      - 4 أرقام
      - يمكن توليد رمز جديد بالضغط على زر "توليد رمز جديد"
   ```

4. **الحفظ**
   - اضغط على زر "إضافة المستخدم" الأزرق
   - أو اضغط "إلغاء" للعودة دون حفظ

### 💡 نصائح:
- يمكن للمستخدم الدخول مباشرة برمز الدخول السريع
- لا حاجة لإدخال اسم مستخدم وكلمة مرور إذا كان رمز الدخول السريع كافياً
- يمكن للمستخدم تسجيل اسم مستخدم وكلمة مرور من حسابه لاحقاً

---

## 🔐 إدارة صلاحيات المستخدم

### الخطوات:

1. **الانتقال لصفحة الصلاحيات**
   - من جدول المستخدمين، اضغط على أيقونة العين 👁️ الزرقاء

2. **الصلاحيات الستة المتاحة**
   ```
   1. ⚙️ الإعدادات العامة
   2. 📅 الجدول المدرسي
   3. 👁️ الإشراف والمناوبة
   4. ⏰ الانتظار اليومي
   5. 📄 نماذج شؤون الطلاب
   6. 💬 الرسائل
   ```

3. **تفعيل/تعطيل الصلاحية**
   - اضغط على بطاقة الصلاحية لتفعيلها
   - ستظهر علامة ✅ خضراء
   - البطاقة ستتحول للون الأزرق

4. **اختيار العمليات المسموح بها**
   عند تفعيل صلاحية، يمكنك اختيار:
   ```
   ☑️ إضافة - يسمح للمستخدم بإضافة بيانات جديدة
   ☑️ تعديل - يسمح بتعديل البيانات الموجودة
   ☑️ حذف - يسمح بحذف البيانات
   ```

5. **حفظ التغييرات**
   - عند إجراء أي تغيير، سيظهر تنبيه أصفر
   - اضغط على زر "حفظ الصلاحيات" الأزرق
   - أو اضغط "إلغاء" للعودة دون حفظ

### 💡 نصائح:
- يمكن تفعيل/تعطيل أي صلاحية بشكل مستقل
- يمكن اختيار العمليات المسموح بها لكل صلاحية
- التغييرات لا تُحفظ إلا بعد الضغط على "حفظ الصلاحيات"

---

## 🔄 تفعيل/تعطيل مستخدم

### الخطوات:

1. **تحديد المستخدم**
   - من جدول المستخدمين، ابحث عن المستخدم المطلوب

2. **تغيير الحالة**
   - اضغط على أيقونة القفل 🔒
   - **إذا كان نشطاً**: سيتحول لغير نشط (قفل برتقالي)
   - **إذا كان غير نشط**: سيتحول لنشط (قفل أخضر مفتوح)

3. **النتيجة**
   - يتغير وضع الحالة في الجدول فوراً
   - المستخدم المعطل لا يمكنه تسجيل الدخول

### 💡 ملاحظة:
- التعطيل لا يحذف المستخدم، بل يمنعه فقط من الدخول
- يمكن إعادة تفعيل المستخدم في أي وقت

---

## 🗑️ حذف مستخدم

### الخطوات:

1. **تحديد المستخدم**
   - من جدول المستخدمين، ابحث عن المستخدم المطلوب

2. **طلب الحذف**
   - اضغط على أيقونة سلة المهملات 🗑️ الحمراء

3. **التأكيد**
   - ستظهر نافذة تأكيد
   - اقرأ التحذير: "لن تتمكن من التراجع عن هذا الإجراء"
   - اضغط "حذف" للتأكيد
   - أو اضغط "إلغاء" للرجوع

### ⚠️ تحذير:
- الحذف نهائي ولا يمكن التراجع عنه
- تأكد من صحة قرارك قبل التأكيد
- يُفضل التعطيل بدلاً من الحذف إذا كنت غير متأكد

---

## 📊 فهم الصفات (Roles)

### 1. مدير المدرسة (Principal) - أحمر 🔴
- المسؤول الأول عن المدرسة
- عادةً يمتلك صلاحيات كاملة
- علامة حمراء مميزة

### 2. وكيل المدرسة (Vice Principal) - أزرق 🔵
- المساعد الأول لمدير المدرسة
- صلاحيات إدارية واسعة
- علامة زرقاء مميزة

### 3. آخر (Other) - رمادي ⚪
- أي مستخدم آخر (معلم، إداري، إلخ)
- صلاحيات محددة حسب الحاجة
- علامة رمادية

---

## 🎨 فهم حالات المستخدم

### ✅ نشط (Active) - أخضر
```
- المستخدم يمكنه تسجيل الدخول
- جميع صلاحياته مفعلة
- يظهر مع علامة ✓ خضراء
```

### ❌ غير نشط (Inactive) - أحمر
```
- المستخدم لا يمكنه تسجيل الدخول
- الصلاحيات معطلة مؤقتاً
- يظهر مع علامة ✗ حمراء
```

---

## 🔑 أنواع تسجيل الدخول

### 1. رمز الدخول السريع (Quick Access PIN)
```
✅ 4 أرقام فقط
✅ سهل الحفظ
✅ سريع الاستخدام
✅ يتم توليده تلقائياً
✅ يمكن تغييره
```

### 2. اسم المستخدم وكلمة المرور
```
✅ أكثر أماناً
✅ يمكن تسجيله لاحقاً
✅ اختياري
✅ يعمل مع رمز الدخول السريع
```

### 💡 توصية:
- استخدم رمز الدخول السريع للدخول السريع اليومي
- سجّل اسم مستخدم وكلمة مرور كنسخة احتياطية

---

## 📱 الاستخدام على الجوال

### ✅ التصميم المتجاوب:
- جميع الصفحات تعمل بشكل ممتاز على الجوال
- الجدول قابل للتمرير أفقياً
- الأزرار كبيرة وسهلة الضغط
- النماذج مرتبة عمودياً للراحة

### 💡 نصائح للجوال:
- استخدم التمرير الأفقي لرؤية جميع أعمدة الجدول
- البطاقات في صفحة الصلاحيات مرتبة عمودياً
- النماذج تتكيف مع حجم الشاشة

---

## ❓ الأسئلة الشائعة

### س: هل يمكن للمستخدم تغيير رمز الدخول السريع الخاص به؟
ج: نعم، يمكن للمستخدم تغييره من إعدادات حسابه، أو يمكن للمدير تغييره عند تعديل بيانات المستخدم.

### س: ماذا يحدث إذا نسي المستخدم رمز الدخول السريع؟
ج: يمكن للمدير توليد رمز جديد من صفحة تعديل المستخدم.

### س: هل يمكن منح صلاحية "تعديل" دون صلاحية "إضافة"؟
ج: نعم، كل عملية مستقلة ويمكن تفعيلها/تعطيلها بشكل منفصل.

### س: ماذا يحدث لصلاحيات المستخدم عند تعطيل حسابه؟
ج: الصلاحيات تبقى كما هي، لكنها تصبح غير فعالة حتى إعادة تفعيل الحساب.

### س: هل يمكن إضافة مستخدم دون اسم مستخدم؟
ج: نعم، يمكن الاكتفاء برمز الدخول السريع. اسم المستخدم اختياري.

### س: كم عدد الصلاحيات المتاحة؟
ج: 6 صلاحيات رئيسية فقط:
   1. الإعدادات العامة
   2. الجدول المدرسي
   3. الإشراف والمناوبة
   4. الانتظار اليومي
   5. نماذج شؤون الطلاب
   6. الرسائل

---

## 🎯 سيناريوهات الاستخدام

### السيناريو 1: إضافة معلم جديد
```
1. اضغط "إضافة مستخدم جديد"
2. أدخل اسم المعلم ورقم جواله
3. اختر الصفة: "آخر"
4. احفظ - سيتم توليد رمز دخول سريع تلقائياً
5. اذهب لصفحة الصلاحيات
6. فعّل صلاحية "الانتظار اليومي" مع عملية "عرض" فقط
7. احفظ الصلاحيات
```

### السيناريو 2: منح وكيل المدرسة صلاحيات واسعة
```
1. اضغط على أيقونة العين بجانب اسم الوكيل
2. فعّل الصلاحيات التالية:
   - الجدول المدرسي (إضافة + تعديل)
   - الإشراف والمناوبة (إضافة + تعديل + حذف)
   - الانتظار اليومي (إضافة + تعديل)
   - نماذج شؤون الطلاب (عرض فقط)
3. احفظ الصلاحيات
```

### السيناريو 3: تعطيل حساب مستخدم مؤقتاً
```
1. ابحث عن المستخدم في الجدول
2. اضغط على أيقونة القفل 🔒
3. سيتحول الحساب لحالة "غير نشط"
4. عندما تريد إعادة التفعيل، اضغط القفل مرة أخرى
```

---

## 🎨 دليل الألوان والأيقونات

### الأيقونات:
- 👁️ **عين زرقاء**: إدارة الصلاحيات
- 🔒 **قفل برتقالي**: تعطيل حساب نشط
- 🔓 **قفل أخضر**: تفعيل حساب معطل
- 🗑️ **سلة حمراء**: حذف نهائي

### الألوان:
- 🔵 **أزرق**: أزرار رئيسية، صلاحيات مفعلة
- 🟢 **أخضر**: حالة نشطة، تفعيل
- 🔴 **أحمر**: حذف، تحذيرات، مدير المدرسة
- 🟠 **برتقالي**: تعطيل
- ⚪ **رمادي**: غير نشط، "آخر"

---

## ✅ قائمة التحقق السريعة

### عند إضافة مستخدم جديد:
- [ ] الاسم الكامل مُدخل
- [ ] رقم الجوال صحيح (05xxxxxxxx)
- [ ] الصفة محددة
- [ ] رمز الدخول السريع ظاهر
- [ ] (اختياري) اسم المستخدم وكلمة المرور

### عند منح الصلاحيات:
- [ ] الصلاحيات المطلوبة مفعلة
- [ ] العمليات المسموح بها محددة (إضافة/تعديل/حذف)
- [ ] تم الضغط على "حفظ الصلاحيات"

### قبل حذف مستخدم:
- [ ] متأكد من الحذف (لا يمكن التراجع)
- [ ] جرّب التعطيل أولاً كبديل
- [ ] قرأت رسالة التحذير

---

## 🆘 المساعدة والدعم

إذا واجهت أي مشكلة:
1. تأكد من أنك مسجل دخول كمدير
2. حدّث الصفحة
3. تحقق من اتصال الإنترنت
4. راجع هذا الدليل
5. اتصل بالدعم الفني

---

**تم التحديث:** أكتوبر 2024  
**الإصدار:** 2.0  
**النظام:** MOTABEA - نظام إدارة المدارس
