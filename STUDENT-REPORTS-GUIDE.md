# دليل استخدام نظام الكشوف - MOTABEA

## 📋 نظرة عامة

تم إضافة ميزة **كشوف الطلاب** إلى نظام متابع، وهي ميزة احترافية تتيح للمعلمين تصميم كشوف مخصصة لمتابعة وتقويم الطلاب بمرونة كاملة.

## ✨ المميزات الرئيسية

### 1. تصميم مرن للكشوف
- إنشاء كشوف مخصصة لكل مادة وفصل
- اختيار عدد الأعمدة المطلوبة
- تسمية الأعمدة حسب الحاجة (واجبات، اختبارات، مشاركة، إلخ)
- تحديد درجات لكل عمود أو تركه بدون درجة

### 2. الأعمدة الأساسية التلقائية
- **العمود الأول**: "م" (الرقم التسلسلي) - يُضاف تلقائياً
- **العمود الثاني**: "اسم الطالب" - يُضاف تلقائياً
- يتم ملء أسماء الطلاب تلقائياً من قاعدة بيانات الفصل المحدد

### 3. إدارة شاملة
- عرض جميع الكشوف المصممة
- البحث والتصفية حسب الفصل أو المادة
- تعديل تصميم الكشف
- حذف الكشوف غير المطلوبة

### 4. تعبئة البيانات
- واجهة سهلة لإدخال البيانات والدرجات
- حساب تلقائي للمجموع الكلي
- حفظ البيانات بشكل آمن

### 5. الطباعة والتصدير
- طباعة الكشف بتنسيق احترافي
- تصدير إلى Excel (قادم قريباً)
- تصميم ملائم للطباعة

## 📍 الموقع في النظام

يوجد قسم "كشوف" في القائمة الجانبية مباشرة تحت قسم "نماذج شؤون الطلاب"

**المسار**: لوحة التحكم ← كشوف

## 🚀 كيفية الاستخدام

### 1. إنشاء كشف جديد

**الخطوات:**
1. انتقل إلى صفحة "كشوف" من القائمة الجانبية
2. اضغط على زر "كشف جديد"
3. املأ المعلومات الأساسية:
   - **اسم الكشف**: مثل "كشف متابعة واجبات الفصل الأول"
   - **الفصل**: اختر الفصل الدراسي
   - **الشعبة**: اختر الشعبة
   - **المادة**: اختر المادة الدراسية

4. صمم الأعمدة:
   - اضغط "إضافة عمود"
   - أدخل اسم العمود (مثل: الواجب الأول، اختبار قصير)
   - حدد إذا كان العمود يحتوي على درجة
   - إذا كان يحتوي على درجة، حدد الدرجة العظمى

5. معاينة التصميم:
   - يظهر في الجانب الأيمن معاينة مباشرة للكشف
   - تأكد من أن التصميم مناسب

6. احفظ الكشف:
   - اضغط "حفظ الكشف"
   - سيتم حفظ التصميم وملء أسماء الطلاب تلقائياً

### 2. عرض وتعبئة الكشف

**الخطوات:**
1. من صفحة الكشوف الرئيسية، اضغط "فتح" على الكشف المطلوب
2. سيظهر الكشف مع أسماء جميع الطلاب
3. للتعبئة:
   - اضغط زر "تعبئة البيانات"
   - أدخل البيانات أو الدرجات في الخلايا
   - يتم حساب المجموع تلقائياً
   - اضغط "حفظ التعديلات"

### 3. طباعة الكشف

**الخطوات:**
1. افتح الكشف المطلوب
2. اضغط زر "طباعة"
3. سيتم عرض الكشف بتنسيق احترافي للطباعة
4. استخدم خيارات الطباعة في المتصفح

### 4. تعديل تصميم الكشف

**الخطوات:**
1. افتح الكشف المطلوب
2. اضغط "تعديل التصميم"
3. عدّل المعلومات أو الأعمدة
4. احفظ التعديلات

### 5. حذف كشف

**الخطوات:**
1. من صفحة الكشوف الرئيسية
2. اضغط أيقونة الحذف (🗑️) على الكشف
3. أكد عملية الحذف

## 💡 أمثلة عملية

### مثال 1: كشف متابعة الواجبات
```
الأعمدة:
- م (تلقائي)
- اسم الطالب (تلقائي)
- الواجب 1 (بدون درجة)
- الواجب 2 (بدون درجة)
- الواجب 3 (بدون درجة)
- ملاحظات (بدون درجة)
```

### مثال 2: كشف الاختبارات القصيرة
```
الأعمدة:
- م (تلقائي)
- اسم الطالب (تلقائي)
- الاختبار القصير 1 (10 درجات)
- الاختبار القصير 2 (10 درجات)
- الاختبار القصير 3 (10 درجات)
- المجموع (يُحسب تلقائياً = 30 درجة)
```

### مثال 3: كشف التقويم الشامل
```
الأعمدة:
- م (تلقائي)
- اسم الطالب (تلقائي)
- المشاركة (5 درجات)
- الواجبات (10 درجات)
- اختبار قصير 1 (10 درجات)
- اختبار قصير 2 (10 درجات)
- الاختبار النهائي (15 درجات)
- المجموع (يُحسب تلقائياً = 50 درجة)
```

## 🔍 البحث والتصفية

### البحث
- ابحث باسم الكشف
- ابحث باسم المادة
- النتائج تظهر فورياً

### التصفية
- **حسب الفصل**: اعرض كشوف فصل معين فقط
- **حسب المادة**: اعرض كشوف مادة معينة فقط
- يمكن الجمع بين الفلترين

## 🎨 واجهة المستخدم

### صفحة الكشوف الرئيسية
- عرض الكشوف على شكل بطاقات احترافية
- كل بطاقة تحتوي على:
  - اسم الكشف والمادة
  - الفصل والشعبة
  - عدد الأعمدة
  - معاينة للأعمدة المصممة
  - أزرار الإجراءات (فتح، تعديل، حذف)

### صفحة التصميم
- قسم المعلومات الأساسية (يمين)
- قسم تصميم الأعمدة (وسط)
- معاينة مباشرة (يسار)

### صفحة العرض والتعبئة
- جدول احترافي مع جميع البيانات
- وضع القراءة ووضع التعديل
- حساب تلقائي للمجاميع
- تنسيق ملائم للطباعة

## 📱 التوافق

### الأجهزة
- ✅ أجهزة الكمبيوتر (مكتبي/محمول)
- ✅ الأجهزة اللوحية
- ✅ الهواتف الذكية

### المتصفحات
- ✅ Chrome
- ✅ Firefox
- ✅ Safari
- ✅ Edge

## 🔒 الأمان والصلاحيات

- يتطلب تسجيل الدخول
- يتم ربط الكشوف بالمدرسة المسجلة
- يمكن للمدير تحديد صلاحيات إنشاء وتعديل الكشوف

## 🛠️ الملفات التقنية

### Frontend
```
src/pages/Dashboard/
  ├── StudentReportsPage.tsx          # الصفحة الرئيسية
  ├── CreateStudentReportPage.tsx     # صفحة التصميم
  └── ViewStudentReportPage.tsx       # صفحة العرض والتعبئة

src/components/Layout/
  └── Sidebar.tsx                     # إضافة القسم في القائمة

src/App.tsx                           # المسارات
```

### Backend
```
server/
  ├── routes/
  │   └── studentReports.js           # API endpoints
  └── index.js                        # تسجيل المسارات
```

## 🔄 API Endpoints

### جلب جميع الكشوف
```
GET /api/student-reports
```

### جلب كشف معين
```
GET /api/student-reports/:id
```

### إنشاء كشف جديد
```
POST /api/student-reports
Body: {
  name, classId, sectionId, subjectId, columns[]
}
```

### تحديث كشف
```
PUT /api/student-reports/:id
Body: {
  name, classId, sectionId, subjectId, columns[]
}
```

### حذف كشف
```
DELETE /api/student-reports/:id
```

### حفظ الدرجات
```
PUT /api/student-reports/:id/grades
Body: {
  grades: { [studentId]: { [columnId]: value } }
}
```

## 🚧 التطويرات المستقبلية

- [ ] تصدير إلى Excel
- [ ] تصدير إلى PDF
- [ ] إرسال الكشوف عبر الواتساب
- [ ] إحصائيات وتقارير شاملة
- [ ] مقارنة أداء الطلاب
- [ ] رسوم بيانية للنتائج
- [ ] نسخ كشف موجود
- [ ] قوالب جاهزة للكشوف

## 📞 الدعم

للحصول على الدعم أو الإبلاغ عن مشاكل:
- استخدم صفحة "الدعم الفني" في النظام
- تواصل مع فريق الدعم

---

**تاريخ الإصدار**: 12 أكتوبر 2025
**الإصدار**: 1.0.0
**نظام متابع - MOTABEA**
