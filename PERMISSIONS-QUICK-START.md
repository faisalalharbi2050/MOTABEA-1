# نظام الصلاحيات - دليل البدء السريع

## 🚀 البدء السريع

### 1. إعداد قاعدة البيانات

```bash
# تشغيل سكريبت قاعدة البيانات
mysql -u root -p motabea < server/database/permissions_schema.sql
```

### 2. تحديث ملف الخادم الخلفي

في ملف `server/index.js`، أضف الكود التالي:

```javascript
// في الأعلى مع باقي الـ requires
const permissionsRoutes = require('./routes/permissions');

// بعد تعريف الراوترات الأخرى (قبل app.listen)
app.use('/api/permissions', permissionsRoutes);
```

### 3. الوصول للنظام

1. شغل الخادم الأمامي:
```bash
npm run dev
```

2. شغل الخادم الخلفي:
```bash
node server/index.js
```

3. افتح المتصفح على: `http://localhost:3000`

4. سجل دخول ثم انتقل إلى: **الصلاحيات** من القائمة الجانبية

## 🎯 حسابات تجريبية

### مدير النظام
- **اسم المستخدم**: `admin`
- **كلمة المرور**: `admin123`
- **رقم الدخول السريع**: `1234`

### وكيل المدرسة
- **اسم المستخدم**: `vice`
- **كلمة المرور**: `vice123`
- **رقم الدخول السريع**: `5678`

### معلم تجريبي
- **اسم المستخدم**: `teacher_demo`
- **كلمة المرور**: `teacher123`
- **رقم الدخول السريع**: `9012`

## 📋 الميزات الرئيسية

### ✅ إدارة المستخدمين
- إضافة مستخدمين من المعلمين أو الإداريين أو يدوياً
- تعديل البيانات والصلاحيات
- تفعيل/تعطيل الحسابات
- حذف المستخدمين

### ✅ نظام الصلاحيات
- 4 مستويات: مدير النظام، مدير، مستخدم، مخصص
- 13 قسم في النظام
- 6 أنواع صلاحيات: عرض، إضافة، تعديل، حذف، تصدير، طباعة

### ✅ الدخول السريع
- رقم من 4 أرقام
- دخول سريع بدون اسم مستخدم وكلمة مرور
- واجهة احترافية مع لوحة أرقام

### ✅ البحث والتصفية
- بحث شامل بالاسم والبريد ورقم الجوال
- تصفية حسب المستوى والمصدر والحالة
- إحصائيات مفصلة

## 🎨 الواجهات المتاحة

### 1. صفحة إدارة الصلاحيات الرئيسية
- عرض جميع المستخدمين
- بطاقات إحصائية
- أدوات بحث وتصفية متقدمة

### 2. نموذج إضافة/تعديل المستخدم
- معلومات أساسية
- معلومات تسجيل الدخول
- اختيار مستوى الصلاحية
- تخصيص تفصيلي للصلاحيات

### 3. نموذج عرض الصلاحيات
- عرض تفصيلي لكل الصلاحيات
- إحصائيات
- سجل النشاط

### 4. نموذج الدخول السريع
- إدخال رقم من 4 أرقام
- لوحة أرقام تفاعلية

## 🔧 استكشاف الأخطاء

### لا تظهر صفحة الصلاحيات؟
1. تأكد من إضافة الراوت في `App.tsx`
2. تأكد من وجود الرابط في `Sidebar.tsx`
3. أعد تشغيل الخادم الأمامي

### خطأ في قاعدة البيانات؟
1. تأكد من تشغيل سكريبت قاعدة البيانات
2. تحقق من اتصال قاعدة البيانات
3. راجع صلاحيات المستخدم في MySQL

### لا يعمل الدخول السريع؟
1. تأكد من إضافة الراوت في الخادم الخلفي
2. تحقق من وجود المستخدمين في قاعدة البيانات
3. تأكد من صحة رقم الدخول السريع

## 📚 المزيد من المعلومات

للحصول على توثيق كامل ومفصل، راجع:
📖 **[PERMISSIONS-SYSTEM-DOCUMENTATION.md](./PERMISSIONS-SYSTEM-DOCUMENTATION.md)**

## 🎯 الخطوات التالية

1. ✅ جرب إضافة مستخدم جديد
2. ✅ امنح صلاحيات مخصصة
3. ✅ جرب الدخول السريع
4. ✅ راجع سجل الصلاحيات
5. ✅ اختبر البحث والتصفية

---

**ملاحظة**: هذا النظام جاهز للاستخدام ولكن يُفضل اختباره في بيئة تطوير أولاً قبل النشر في بيئة الإنتاج.

✨ **استمتع باستخدام نظام الصلاحيات الاحترافي!**
