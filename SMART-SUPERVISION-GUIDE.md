# 📋 دليل نظام الإشراف اليومي الذكي - MOTABEA

## 🎯 نظرة عامة

تم تطوير **نظام الإشراف اليومي الذكي** كجزء من منصة MOTABEA لإدارة المدارس. النظام يوفر حلاً متكاملاً لتوزيع المشرفين تلقائياً على أيام الأسبوع مع مراعاة جداول المعلمين والقيود المختلفة.

## ✨ المميزات الرئيسية

### 1. التوزيع التلقائي الذكي
- **خوارزمية ذكية** لتوزيع المشرفين تلقائياً
- **مراعاة جداول المعلمين** والحصص الفارغة
- **قيد يوم واحد** لكل معلم في الأسبوع
- **استثناء المعلمين** المشغولين أو المعفيين

### 2. إدارة الإعدادات
- تحديد عدد المشرفين لكل يوم
- إدارة قائمة المستثنين من الإشراف
- تخصيص مواقع الإشراف
- إعدادات مواعيد الفسح

### 3. نظام المتابعة والتفعيل
- تسجيل حضور المشرفين
- حالات متعددة: حاضر، غائب، مستأذن، متأخر
- زر تحضير سريع للكل
- واجهة سهلة للمتابعة اليومية

### 4. التقارير والإحصائيات
- إحصائيات شاملة للحضور والغياب
- نسب الالتزام
- تقارير حسب اليوم
- رسوم بيانية تفاعلية

### 5. التواصل السريع
- إرسال رسائل واتساب تلقائياً
- إشعارات للمشرفين
- رسائل مخصصة لكل معلم

### 6. الطباعة
- تصميم احترافي جاهز للطباعة
- إخفاء العناصر غير الضرورية عند الطباعة
- دعم كامل للغة العربية

## 🎨 التصميم

### الألوان المستخدمة
- **اللون الأساسي**: `#4f46e5` (Indigo)
- **اللون الثانوي**: `#6366f1` (Indigo Light)
- **اللون الثالث**: `#818cf8` (Indigo Lighter)

### المكونات
- **شريط العنوان**: مطابق لتصميم صفحة إنشاء الجدول
- **شريط الأدوات**: أزرار عصرية مع أيقونات معبرة
- **التبويبات**: لعرض الفسح المختلفة
- **البطاقات**: لعرض أسماء المشرفين بشكل جذاب

## 🔧 البنية التقنية

### Frontend
- **React 18.3.1** مع TypeScript
- **Tailwind CSS** للتنسيق
- **Radix UI** للمكونات
- **Lucide React** للأيقونات

### Backend (مقترح)
- **Node.js** مع Express.js
- **MySQL** لقاعدة البيانات
- **RESTful API** للتواصل

### الملفات الرئيسية

#### 1. الواجهة الأمامية
```
src/pages/Dashboard/Supervision/SmartDailySupervisionPage.tsx
```
- الصفحة الرئيسية للنظام
- تحتوي على جميع الواجهات والمكونات
- الخوارزمية الذكية للتوزيع

#### 2. الواجهة الخلفية
```
server/routes/supervision.js
```
- نقاط النهاية (Endpoints) للـ API
- معالجة الطلبات والاستجابات
- التعامل مع قاعدة البيانات

#### 3. قاعدة البيانات
```
database/supervision_schema.sql
```
- هيكل الجداول
- العلاقات والمفاتيح
- الإجراءات المخزنة (Stored Procedures)

## 📊 قاعدة البيانات

### الجداول الرئيسية

#### 1. supervision_settings
إعدادات النظام العامة
- عدد المشرفين لكل يوم
- مواعيد الفسح
- مواقع الإشراف

#### 2. supervision_exclusions
قائمة المستثنين من الإشراف
- معرف المعلم
- السنة والفصل الدراسي
- سبب الاستثناء

#### 3. supervision_slots
جدول مواعيد الإشراف الأساسي
- اليوم والفسحة
- المعلم المشرف
- الموقع
- الحالة

#### 4. supervision_attendance
سجلات الحضور والغياب
- معرف الموعد
- الحالة (حاضر/غائب/...)
- وقت التسجيل
- ملاحظات

#### 5. supervision_audit_log
سجل التغييرات
- نوع العملية
- القيم القديمة والجديدة
- من قام بالتغيير

#### 6. supervision_notifications
الإشعارات والتنبيهات
- نوع الإشعار
- طريقة الإرسال
- حالة القراءة

## 🚀 كيفية الاستخدام

### 1. إعداد النظام

#### الخطوة الأولى: إعدادات الإشراف
1. افتح صفحة الإشراف اليومي الذكي
2. اضغط على زر "إعدادات الإشراف"
3. حدد عدد المشرفين المطلوب لكل يوم (مثلاً: 8)
4. اختر المعلمين المستثنين من قائمة المعلمين
5. أدخل مواقع الإشراف (كل موقع في سطر)
6. اضغط "حفظ الإعدادات"

#### الخطوة الثانية: إنشاء الجدول
1. اضغط على زر "إنشاء جدول الإشراف"
2. ستعمل الخوارزمية الذكية تلقائياً على:
   - تحليل جداول المعلمين
   - البحث عن الحصص الفارغة
   - توزيع المشرفين بالتساوي
   - تطبيق القيود (يوم واحد لكل معلم)
3. سيظهر الجدول في التبويبات

### 2. المتابعة اليومية

#### متابعة تفعيل الإشراف
1. اضغط على زر "متابعة التفعيل"
2. اختر اليوم والفسحة
3. سجل حالة كل مشرف:
   - ✅ حاضر
   - ❌ غائب
   - ⚠️ مستأذن
   - 🕒 متأخر
4. استخدم "تحضير الكل" للتسجيل السريع

### 3. التقارير

#### عرض التقارير والإحصائيات
1. اضغط على زر "تقارير الإشراف"
2. شاهد:
   - إجمالي المواعيد
   - عدد الحاضرين والغائبين
   - نسبة الالتزام
   - التفاصيل حسب اليوم
3. اضغط "تصدير التقرير" لحفظ البيانات

### 4. الإشعارات

#### إرسال رسائل واتساب
1. اضغط على زر "إرسال (واتساب)"
2. سيتم فتح روابط واتساب لكل مشرف
3. الرسالة تتضمن:
   - اسم المعلم
   - يوم الإشراف
   - موقع الإشراف

### 5. الطباعة
1. اضغط على زر "طباعة"
2. سيتم إخفاء العناصر غير الضرورية تلقائياً
3. اختر الطابعة وقم بالطباعة

## 🔐 الخوارزمية الذكية

### آلية العمل

```javascript
function smartDistributeSupervisors() {
  // 1. تهيئة المتغيرات
  const newSlots = [];
  const usedTeachers = new Set(); // المعلمين المستخدمين
  
  // 2. لكل يوم في الأسبوع
  for (let dayIndex = 0; dayIndex < 5; dayIndex++) {
    
    // 3. لكل فسحة
    for (const breakConfig of breakTiming) {
      
      // 4. الحصول على المعلمين المؤهلين
      const eligibleTeachers = getEligibleTeachers(
        dayIndex, 
        breakConfig.afterPeriod, 
        usedTeachers
      );
      
      // 5. توزيع المشرفين
      for (let i = 0; i < supervisorsPerDay; i++) {
        if (eligibleTeachers[i]) {
          // إضافة المشرف
          newSlots.push(createSlot(eligibleTeachers[i]));
          // تسجيل استخدام المعلم
          usedTeachers.add(eligibleTeachers[i].id);
        }
      }
    }
  }
  
  return newSlots;
}
```

### شروط اختيار المعلم

#### 1. قيد التفرغ
```javascript
// المعلم يجب أن يكون لديه حصة فارغة
const hasFreePeriodBefore = !schedule[dayIndex][periodBefore];
const hasFreePeriodAfter = !schedule[dayIndex][periodAfter];
return hasFreePeriodBefore || hasFreePeriodAfter;
```

#### 2. قيد التكرار
```javascript
// المعلم يشرف يوم واحد فقط في الأسبوع
if (usedTeachers.has(teacher.id)) {
  return false;
}
```

#### 3. قيد الاستثناء
```javascript
// استبعاد المعلمين المستثنين
if (excludedTeachers.includes(teacher.id)) {
  return false;
}
```

## 🌐 API Endpoints

### 1. الإعدادات

#### GET /api/supervision/settings
الحصول على إعدادات الإشراف

**Response:**
```json
{
  "success": true,
  "data": {
    "supervisorsPerDay": 8,
    "excludedTeachers": ["1", "5"],
    "breakTiming": [
      { "breakNumber": 1, "afterPeriod": 2 },
      { "breakNumber": 2, "afterPeriod": 4 }
    ],
    "locations": ["الساحة الشرقية", "الساحة الغربية"]
  }
}
```

#### POST /api/supervision/settings
حفظ إعدادات الإشراف

**Request Body:**
```json
{
  "supervisorsPerDay": 8,
  "excludedTeachers": ["1", "5"],
  "locations": ["الساحة الشرقية", "الساحة الغربية"]
}
```

### 2. الجدول

#### GET /api/supervision/schedule
الحصول على جدول الإشراف

**Query Parameters:**
- `weekStart`: تاريخ بداية الأسبوع
- `weekEnd`: تاريخ نهاية الأسبوع

#### POST /api/supervision/schedule/generate
إنشاء جدول الإشراف تلقائياً

**Request Body:**
```json
{
  "weekStart": "2024-11-24"
}
```

**Response:**
```json
{
  "success": true,
  "message": "تم إنشاء الجدول بنجاح",
  "data": {
    "slots": [...],
    "totalAssigned": 80,
    "totalNeeded": 80
  }
}
```

#### DELETE /api/supervision/schedule
حذف جدول الإشراف

**Query Parameters:**
- `weekStart`: تاريخ بداية الأسبوع

### 3. الحضور

#### POST /api/supervision/attendance
تحديث حالة الحضور

**Request Body:**
```json
{
  "supervisionSlotId": "0-1-123",
  "status": "present",
  "notes": ""
}
```

### 4. التقارير

#### GET /api/supervision/reports/statistics
الحصول على إحصائيات الإشراف

**Response:**
```json
{
  "success": true,
  "data": {
    "overall": {
      "total": 80,
      "present": 72,
      "absent": 5,
      "presentPercentage": "90.00"
    },
    "byDay": [...]
  }
}
```

## 📱 واجهة المستخدم

### المكونات الرئيسية

#### 1. شريط العنوان
```tsx
<div className="bg-gradient-to-r from-[#4f46e5] to-[#6366f1]">
  <Shield className="h-6 w-6 text-white" />
  <h1>جدول الإشراف اليومي الذكي</h1>
</div>
```

#### 2. شريط الأدوات
```tsx
<Button className="bg-gradient-to-r from-[#4f46e5] to-[#6366f1]">
  <Zap className="h-5 w-5" />
  إنشاء جدول الإشراف
</Button>
```

#### 3. التبويبات
```tsx
<Tabs>
  <TabsList>
    <TabsTrigger value="break-1">الفسحة الأولى</TabsTrigger>
    <TabsTrigger value="break-2">الفسحة الثانية</TabsTrigger>
  </TabsList>
</Tabs>
```

#### 4. الجدول
```tsx
<table>
  <thead>
    <tr className="bg-gradient-to-r from-[#4f46e5] to-[#6366f1]">
      <th>اليوم</th>
      <th>المشرفون</th>
      <th>موقع الإشراف</th>
    </tr>
  </thead>
</table>
```

## 🔄 التكامل مع النظام

### 1. الربط مع قاعدة بيانات المعلمين
```javascript
// جلب المعلمين من قاعدة البيانات
const teachers = await db.query('SELECT * FROM teachers');
```

### 2. الربط مع نظام الجداول
```javascript
// جلب جدول حصص المعلم
const timetable = await db.query(
  'SELECT * FROM timetable WHERE teacher_id = ?',
  [teacherId]
);
```

### 3. الربط مع نظام المصادقة
```javascript
// التحقق من صلاحيات المستخدم
const hasPermission = await checkPermission(userId, 'manage_supervision');
```

## 🐛 استكشاف الأخطاء

### مشكلة: لا يظهر الجدول بعد الإنشاء
**الحل:**
1. تحقق من البيانات في `supervisionSlots`
2. تأكد من وجود معلمين مؤهلين
3. راجع الإعدادات والاستثناءات

### مشكلة: عدد المشرفين أقل من المطلوب
**الحل:**
1. قلل عدد المشرفين في الإعدادات
2. قلل عدد المستثنين
3. تأكد من جداول المعلمين

### مشكلة: لا تعمل رسائل واتساب
**الحل:**
1. تحقق من أرقام الهواتف
2. تأكد من صيغة الأرقام الدولية
3. راجع إعدادات المتصفح

## 📈 التطويرات المستقبلية

### مميزات مقترحة
1. **التكامل مع تقويم Google**
2. **إشعارات تلقائية عبر SMS**
3. **تطبيق جوال مخصص**
4. **تقارير متقدمة مع Excel**
5. **لوحة تحكم تفاعلية**
6. **نظام نقاط للمشرفين**
7. **تقييم أداء المشرفين**

## 🤝 المساهمة

للمساهمة في تطوير النظام:
1. Fork المشروع
2. إنشاء branch جديد
3. تنفيذ التعديلات
4. إرسال Pull Request

## 📞 الدعم

للحصول على الدعم:
- **البريد الإلكتروني**: support@motabea.com
- **الموقع**: https://motabea.com
- **الهاتف**: +966 XX XXX XXXX

## 📄 الترخيص

هذا النظام جزء من منصة MOTABEA ويخضع لشروط الاستخدام الخاصة بالشركة.

---

**تم التطوير بواسطة**: فريق تطوير MOTABEA  
**التاريخ**: نوفمبر 2024  
**الإصدار**: 1.0.0  
**اللغة**: العربية  
**التقنيات**: React, TypeScript, Tailwind CSS, Node.js, MySQL

---

## 🎓 ملاحظات مهمة

1. **الأمان**: تأكد من تأمين نقاط النهاية بنظام مصادقة قوي
2. **الأداء**: استخدم Pagination للجداول الكبيرة
3. **النسخ الاحتياطي**: احتفظ بنسخ احتياطية من البيانات
4. **الاختبار**: اختبر النظام بشكل شامل قبل الإنتاج
5. **التوثيق**: احتفظ بتوثيق محدث للتغييرات

---

**شكراً لاستخدام نظام الإشراف اليومي الذكي! 🚀**
