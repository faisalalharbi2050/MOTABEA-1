# 🎨 دليل الألوان والأيقونات - المرحلة السادسة

---

## 🎨 الألوان المعتمدة

### الألوان الأساسية

```css
/* اللون الأساسي - Indigo 600 */
#4f46e5

/* اللون الثانوي - Indigo 500 */
#6366f1

/* اللون الإضافي - Indigo 400 */
#818cf8
```

### استخدامات الألوان

#### 1. الأزرار
```tsx
className="bg-gradient-to-r from-[#4f46e5] to-[#6366f1]"
```

#### 2. هيدر الجداول
```tsx
className="bg-gradient-to-r from-[#4f46e5] to-[#6366f1] text-white"
```

#### 3. هيدر الحوارات
```tsx
className="bg-gradient-to-r from-[#4f46e5] to-[#6366f1] text-white"
```

#### 4. النصوص المميزة
```tsx
className="text-[#4f46e5] font-bold"
```

#### 5. الحدود والبطاقات
```tsx
className="border-[#4f46e5] bg-blue-50"
```

---

## 🎯 الأيقونات المستخدمة

### من lucide-react

```tsx
import { 
  Eye,         // عرض
  Trash,       // حذف جدول
  Trash2,      // حذف الكل
  Edit2,       // تعديل
  Save,        // حفظ
  X,           // إغلاق
  Calendar,    // تقويم
  Clock,       // ساعة
  CheckCircle, // نجاح
  AlertCircle, // تحذير/خطأ
  Info         // معلومات
} from 'lucide-react';
```

### استخدام الأيقونات

#### 1. أيقونة العين (عرض الجدول)
```tsx
<Eye className="w-5 h-5" />
```
- **اللون**: أبيض على خلفية زرقاء
- **الحجم**: 5x5 (20px)
- **الوظيفة**: فتح حوار العرض/التعديل

#### 2. أيقونة الحذف (سلة المهملات)
```tsx
<Trash className="w-5 h-5" />
```
- **اللون**: أبيض على خلفية حمراء
- **الحجم**: 5x5 (20px)
- **الوظيفة**: حذف جدول واحد

#### 3. أيقونة التعديل
```tsx
<Edit2 className="w-5 h-5" />
```
- **اللون**: أبيض
- **الموضع**: زر تعديل الجدول

#### 4. أيقونة الحفظ
```tsx
<Save className="w-5 h-5" />
```
- **اللون**: أبيض على خلفية خضراء
- **الموضع**: زر حفظ التعديلات

#### 5. أيقونة الإغلاق
```tsx
<X className="w-6 h-6" />
أو
<X className="w-4 h-4" />
```
- **الحجم**: حسب الموضع
- **الوظيفة**: إغلاق الحوارات والإشعارات

---

## 🔔 ألوان الإشعارات

### 1. إشعار النجاح (Success)
```css
/* الخلفية */
bg-green-50

/* الحدود */
border-green-500

/* النص */
text-green-800

/* الأيقونة */
text-green-600
```

### 2. إشعار الخطأ (Error)
```css
/* الخلفية */
bg-red-50

/* الحدود */
border-red-500

/* النص */
text-red-800

/* الأيقونة */
text-red-600
```

### 3. إشعار التحذير (Warning)
```css
/* الخلفية */
bg-yellow-50

/* الحدود */
border-yellow-500

/* النص */
text-yellow-800

/* الأيقونة */
text-yellow-600
```

### 4. إشعار المعلومات (Info)
```css
/* الخلفية */
bg-blue-50

/* الحدود */
border-blue-500

/* النص */
text-blue-800

/* الأيقونة */
text-blue-600
```

---

## 🎨 تدرجات الألوان

### تدرج الأزرار الأساسية
```css
/* العادي */
from-[#4f46e5] to-[#6366f1]

/* عند الـ hover */
from-[#4338ca] to-[#4f46e5]
```

### تدرج أزرار الحذف
```css
/* العادي */
from-red-600 to-red-700

/* عند الـ hover */
from-red-700 to-red-800
```

### تدرج أزرار النجاح
```css
/* العادي */
from-green-600 to-green-700

/* عند الـ hover */
from-green-700 to-green-800
```

---

## 🎭 ألوان المشرفين (في الجدول)

### الجهة اليمنى
```css
/* الخلفية */
bg-blue-50

/* الحدود */
border-blue-200

/* الشارة */
bg-blue-500 text-white
```

### الجهة اليسرى
```css
/* الخلفية */
bg-purple-50

/* الحدود */
border-purple-200

/* الشارة */
bg-purple-500 text-white
```

---

## 📐 الأحجام والمقاسات

### أحجام الأيقونات
```tsx
// صغير
className="w-4 h-4"  // 16px

// متوسط
className="w-5 h-5"  // 20px

// كبير
className="w-6 h-6"  // 24px
```

### أحجام الأزرار
```tsx
// ارتفاع الأزرار في شريط العمليات
className="py-3"  // padding-y: 12px

// ارتفاع الأزرار في الحوارات
className="py-2"  // padding-y: 8px
```

### مقاسات الحوارات
```tsx
// صغير
className="max-w-md"   // 448px

// متوسط
className="max-w-2xl"  // 672px

// كبير
className="max-w-6xl"  // 1152px
```

---

## 🌈 تأثيرات الـ hover

### الأزرار
```css
/* تغيير التدرج */
hover:from-[#4338ca] hover:to-[#4f46e5]

/* تغيير الظل */
hover:shadow-lg

/* الانتقال السلس */
transition-all duration-200
```

### صفوف الجدول
```css
hover:bg-blue-50
```

### الأيقونات
```css
hover:bg-white/20
```

---

## 🎨 نموذج كود كامل

### زر مع أيقونة
```tsx
<button
  className="px-6 py-3 bg-gradient-to-r from-[#4f46e5] to-[#6366f1] 
             hover:from-[#4338ca] hover:to-[#4f46e5] 
             text-white rounded-lg transition-all shadow-lg 
             flex items-center gap-2"
>
  <Eye className="w-5 h-5" />
  <span>عرض</span>
</button>
```

### هيدر جدول
```tsx
<thead>
  <tr className="bg-gradient-to-r from-[#4f46e5] to-[#6366f1] text-white">
    <th className="px-4 py-3 text-center font-bold border-l border-white/20">
      العنوان
    </th>
  </tr>
</thead>
```

### بطاقة معلومات
```tsx
<div className="bg-gradient-to-r from-blue-50 to-indigo-50 
                border-2 border-[#4f46e5] rounded-xl p-4">
  <p className="text-[#4f46e5] font-bold">المحتوى</p>
</div>
```

---

## 📝 ملاحظات مهمة

1. **استخدام الـ hex colors في Tailwind**:
   ```tsx
   className="bg-[#4f46e5]"
   // أو
   className="from-[#4f46e5] to-[#6366f1]"
   ```

2. **التدرجات دائماً من-إلى**:
   ```tsx
   from-[color1] to-[color2]
   ```

3. **الشفافية**:
   ```tsx
   className="bg-white/20"  // 20% opacity
   className="border-white/20"
   ```

4. **التحولات السلسة**:
   ```tsx
   className="transition-all duration-200"
   ```

---

## 🎯 نصائح للتطوير المستقبلي

1. **الثبات**: استخدم نفس الألوان في كل الصفحات
2. **التناسق**: نفس أحجام الأيقونات لنفس الوظائف
3. **الوضوح**: ألوان متباينة للنصوص على الخلفيات
4. **الأداء**: استخدم Tailwind بدلاً من CSS مخصص

---

*مرجع سريع - المرحلة السادسة*
*تاريخ الإنشاء: 26 نوفمبر 2025*
