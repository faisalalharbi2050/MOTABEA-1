# 🚀 MOTABEA - نظام إدارة المدرسة

## 📋 نظرة عامة
MOTABEA نظام شامل لإدارة المدارس بتقنيات حديثة، يوفر حلولاً متكاملة لإدارة المعلمين، الطلاب، الجداول، الإشراف، والشؤون الأكاديمية.

---

## ⚡ التشغيل السريع

### 🎯 الطريقة الأسهل (موصى بها)
فقط قم بالنقر المزدوج على:
```
start-project.bat
```

سيتم تشغيل الخادم الخلفي والواجهة الأمامية تلقائياً! 🎉

---

## 🛠️ التثبيت والإعداد

### المتطلبات الأساسية
- ✅ **Node.js** (الإصدار 16 أو أحدث) - [تحميل](https://nodejs.org)
- ✅ **npm** (يأتي مع Node.js)

### خطوات التثبيت

1️⃣ **تحميل المشروع**
```bash
git clone [repository-url]
cd MOTABEA-1
```

2️⃣ **تثبيت المكتبات**
```bash
npm install
```

3️⃣ **تشغيل المشروع**

**الخيار 1: تشغيل كامل (موصى به)**
```bash
start-project.bat
```
أو
```bash
npm run start:all
```

**الخيار 2: تشغيل منفصل**

في نافذة Terminal الأولى:
```bash
start-server.bat
```
أو
```bash
node server/index.js
```

في نافذة Terminal الثانية:
```bash
npm run dev
```

---

## 🌐 الوصول للنظام

### 🔗 الروابط
- **الواجهة الأمامية**: http://localhost:3003
- **الخادم الخلفي**: http://localhost:5001

### 🔑 حسابات تجريبية

| الدور | اسم المستخدم | كلمة المرور | الصلاحيات |
|------|-------------|-------------|-----------|
| 👨‍💼 **مدير** | `admin` | `admin123` | جميع الصلاحيات |
| 👨‍🏫 **وكيل** | `vice` | `vice123` | صلاحيات إدارية محدودة |
| 👨‍🔧 **مشرف** | `supervisor` | `super123` | صلاحيات الإشراف |

---

## 📁 هيكل المشروع

```
MOTABEA-1/
├── 📂 src/                    # الواجهة الأمامية (React + TypeScript)
│   ├── components/           # المكونات القابلة لإعادة الاستخدام
│   ├── pages/                # صفحات التطبيق
│   ├── types/                # أنواع TypeScript
│   └── ...
├── 📂 server/                 # الخادم الخلفي (Node.js + Express)
│   ├── index.js              # نقطة البداية للخادم
│   └── mockDatabase.js       # قاعدة البيانات التجريبية
├── 📂 public/                 # الملفات الثابتة
├── 📄 start-project.bat      # 🌟 تشغيل كامل المشروع
├── 📄 start-server.bat       # تشغيل الخادم فقط
├── 📄 package.json
└── 📄 vite.config.ts
```

---

## 🎨 التقنيات المستخدمة

### Frontend
- ⚛️ **React** 18.3.1
- 📘 **TypeScript** 5.5.3
- ⚡ **Vite** 5.4.1
- 🎨 **Tailwind CSS** 3.4.11
- 🧩 **Radix UI** - مكونات UI
- 🎭 **Framer Motion** - الحركات والانتقالات

### Backend
- 🟢 **Node.js**
- 🚂 **Express.js** 4.18.2
- 🔐 **JWT** + **bcrypt** - المصادقة

### مكتبات إضافية
- 📊 **Chart.js** - الرسوم البيانية
- 📑 **XLSX.js** - تصدير/استيراد Excel
- 📄 **jsPDF** - إنشاء PDF
- 🔄 **React Hook Form** - إدارة النماذج

---

## 🎯 المميزات الرئيسية

### ✨ إدارة شاملة
- 👥 **إدارة المعلمين** - ملفات كاملة، تقييمات، حضور
- 🎓 **إدارة الطلاب** - سجلات، درجات، تواصل
- 📅 **الجداول الدراسية** - إنشاء وتعديل تلقائي
- 📊 **التقارير** - تقارير مفصلة قابلة للطباعة

### 🔐 نظام مصادقة متقدم
- تسجيل دخول آمن
- إدارة صلاحيات حسب الدور
- JWT tokens

### 📱 تصميم متجاوب
- يعمل على جميع الأجهزة
- واجهة عربية (RTL)
- تصميم احترافي حديث

### 📤 تصدير واستيراد
- Excel (XLSX)
- PDF
- طباعة مباشرة

---

## 🔧 الأوامر المتاحة

### التطوير
```bash
npm run dev              # تشغيل الواجهة الأمامية
npm run start:server     # تشغيل الخادم الخلفي
npm run start:all        # تشغيل كلاهما
```

### البناء
```bash
npm run build           # بناء للإنتاج
npm run preview         # معاينة النسخة المبنية
```

### الاختبار
```bash
npm run lint           # فحص الكود
```

---

## 🐛 حل المشاكل الشائعة

### ❌ المشكلة: "لا يمكن الاتصال بالخادم"
**الحل:**
1. تأكد من تشغيل الخادم الخلفي:
   ```bash
   start-server.bat
   ```
2. تحقق من المنفذ 5001:
   ```bash
   netstat -ano | findstr :5001
   ```
3. إعادة تشغيل كامل المشروع:
   ```bash
   start-project.bat
   ```

### ❌ المشكلة: "Port already in use"
**الحل:**
```bash
# إيقاف العملية على المنفذ 5001
netstat -ano | findstr :5001
taskkill /F /PID [رقم_العملية]

# إيقاف العملية على المنفذ 3003
netstat -ano | findstr :3003
taskkill /F /PID [رقم_العملية]
```

### ❌ المشكلة: "Module not found"
**الحل:**
```bash
# حذف المكتبات القديمة
rm -rf node_modules package-lock.json

# إعادة التثبيت
npm install
```

### ❌ المشكلة: "لا توجد مدارس في النظام"
**الحل:**
- تأكد من تشغيل الخادم الخلفي أولاً
- البيانات التجريبية موجودة في `server/mockDatabase.js`
- تحديث الصفحة بعد تشغيل الخادم

---

## 📝 ملاحظات مهمة

### ⚠️ تحذيرات
- 🚫 **لا تغلق** نوافذ الخوادم أثناء الاستخدام
- 💾 البيانات الحالية **مؤقتة** (mockDatabase)
- 🔄 عند إعادة تشغيل الخادم تُفقد التعديلات

### 💡 نصائح
- استخدم `start-project.bat` للتشغيل السريع
- افتح نافذتي Terminal منفصلتين للتطوير
- راجع console في المتصفح للأخطاء

---

## 🎨 التخصيص

### تغيير الألوان
ملف: `tailwind.config.js`
```js
theme: {
  extend: {
    colors: {
      primary: '#your-color',
      secondary: '#your-color',
    }
  }
}
```

### تغيير المنافذ
ملف: `vite.config.ts` للـ Frontend
```ts
server: {
  port: 3003  // غيّر هنا
}
```

ملف: `server/index.js` للـ Backend
```js
const PORT = 5001;  // غيّر هنا
```

---

## 📞 الدعم والمساعدة

### 📚 الموارد
- 📖 [دليل المستخدم](./ASSIGNMENT-QUICK-USER-GUIDE-V2.md)
- 👨‍💻 [دليل المطور](./DEVELOPER-GUIDE-PRINT.md)
- 📊 [لوحة التحكم](./DASHBOARD-README.md)

### 🐞 الإبلاغ عن مشكلة
إذا واجهت مشكلة:
1. تحقق من قسم "حل المشاكل الشائعة"
2. راجع سجلات الأخطاء في console
3. تأكد من تحديث المكتبات: `npm install`

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر للاستخدام التعليمي والتطويري.

---

## 🚀 الإصدار الحالي

**النسخة:** 1.0.0  
**آخر تحديث:** نوفمبر 2025  
**الحالة:** ✅ جاهز للاستخدام

---

## 🌟 شكر خاص

تم تطوير هذا النظام باستخدام أفضل الممارسات والتقنيات الحديثة لتوفير تجربة مستخدم ممتازة.

---

<div align="center">

### صُنع بـ ❤️ لتطوير التعليم

**MOTABEA © 2025**

</div>
