# نظام إسناد المواد - MOTABEA Assignment System

## نظرة عامة

نظام إسناد المواد هو جزء من منصة MOTABEA لإدارة المدارس، مصمم لإدارة توزيع المواد الدراسية على المعلمين والفصول بطريقة تفاعلية وفعالة.

## الميزات الرئيسية

### 🏗️ البنية الأساسية
- **إدارة شاملة للبيانات**: معلمين، مواد، فصول، وإسناد
- **نظام حالة متقدم**: إدارة الحالة باستخدام useReducer و Context API
- **تخزين محلي**: حفظ البيانات محلياً مع نظام النسخ الاحتياطي
- **التراجع والإعادة**: نظام undo/redo لجميع العمليات

### 📱 واجهة المستخدم التفاعلية
- **شريط الأدوات**: أدوات سريعة للوصول للوظائف الأساسية
- **شريط التحديد**: اختيار متعدد مع عمليات مجمعة
- **سكة المعلمين**: عرض وإدارة المعلمين مع معلومات العبء
- **مصفوفة المواد**: عرض تفاعلي لتوزيع المواد على الفصول
- **البطاقات التفاعلية**: عرض تفصيلي لكل مادة مع إمكانية التحرير

### 📊 نظام التقارير المتطور
- **تقارير المعلمين**: تقارير فردية شاملة لكل معلم
- **تقارير الخطة**: نظرة عامة على توزيع الإسناد
- **إحصائيات متقدمة**: تحليل العبء التدريسي والتغطية
- **مخططات بيانية**: عرض مرئي للبيانات والإحصائيات

### 📤 أدوات التصدير والمشاركة
- **تصدير CSV**: جداول بيانات قابلة للتحرير
- **تصدير PDF**: تقارير احترافية للطباعة
- **تصدير HTML**: ملفات مكتفية ذاتياً لمتصفحات الويب
- **مشاركة WhatsApp**: رسائل مخصصة للمعلمين والإدارة
- **مشاركة أصلية**: استخدام Web Share API

## البنية الفنية

### 📁 هيكل المشروع

```
src/features/assignment/
├── store/                      # إدارة الحالة
│   ├── types.ts               # تعريف الأنواع والواجهات
│   ├── assignmentStore.tsx    # المخزن الرئيسي باستخدام Context API
│   ├── selectors.ts          # محددات البيانات المشتقة
│   ├── validators.ts         # وظائف التحقق من صحة البيانات
│   └── undoRedo.ts          # نظام التراجع والإعادة
├── components/               # مكونات واجهة المستخدم
│   ├── ActionBar.tsx        # شريط الأدوات الرئيسي
│   ├── SelectionBar.tsx     # شريط التحديد المتعدد
│   ├── TeacherRail.tsx      # سكة المعلمين
│   ├── SubjectMatrix.tsx    # مصفوفة المواد والفصول
│   ├── SubjectCard.tsx      # بطاقة المادة التفاعلية
│   ├── AssignInlinePicker.tsx # أداة الإسناد المباشر
│   ├── WhatsAppMenu.tsx     # قائمة الواتساب
│   ├── PdfMenu.tsx          # قائمة تصدير PDF
│   ├── HtmlExportAllMenu.tsx # قائمة تصدير HTML
│   ├── Snackbar.tsx         # إشعارات النظام
│   ├── LoadBar.tsx          # شريط التحميل
│   └── Legend.tsx           # مفتاح الألوان والرموز
├── reports/                  # نظام التقارير
│   ├── TeacherReportView.tsx # تقرير المعلم الفردي
│   ├── PlanReportView.tsx   # تقرير خطة الإسناد
│   ├── print.css           # أنماط الطباعة
│   └── index.ts            # فهرس التقارير
├── export/                   # أدوات التصدير
│   ├── htmlAllBuilder.ts    # بناء ملفات HTML
│   ├── htmlAllDownload.ts   # تنزيل HTML
│   └── index.ts            # فهرس التصدير
├── utils/                    # الأدوات المساعدة
│   ├── csv.ts              # تصدير CSV
│   ├── PdfClient.ts        # عميل PDF
│   ├── whatsapp.ts         # أدوات الواتساب
│   └── index.ts            # فهرس الأدوات
├── styles/                   # الأنماط
│   └── assignment.css      # أنماط النظام الرئيسية
├── AssignmentPage.tsx        # الصفحة الرئيسية
└── index.ts                 # الفهرس الرئيسي
```

### 🔧 التقنيات المستخدمة

- **React 18.3.1** مع **TypeScript 5.5.3**
- **Context API** و **useReducer** لإدارة الحالة
- **React Router DOM** للتنقل
- **Tailwind CSS** للتنسيق مع دعم RTL
- **jsPDF** لإنشاء ملفات PDF
- **Web APIs** للمشاركة والحافظة

## نظام إدارة الحالة

### الأنواع الأساسية

```typescript
interface Teacher {
  id: ID;
  name: string;
  specialization: string;
  maxLoad: number;
  currentLoad: number;
  isActive: boolean;
}

interface Subject {
  id: ID;
  name: string;
  code: string;
  requiredHours: number;
  level: 'primary' | 'middle' | 'high';
  isActive: boolean;
}

interface Classroom {
  id: ID;
  name: string;
  grade: string;
  capacity: number;
  currentStudents: number;
  level: 'primary' | 'middle' | 'high';
  isActive: boolean;
}

interface Assignment {
  id: ID;
  teacherId: ID;
  subjectId: ID;
  classroomId: ID;
  hoursPerWeek: number;
  status: 'active' | 'pending' | 'cancelled';
  notes?: string;
}
```

### الإجراءات المدعومة

- **إدارة المعلمين**: إضافة، تحديث، حذف، تبديل الحالة
- **إدارة المواد**: إضافة، تحديث، حذف، تبديل الحالة
- **إدارة الفصول**: إضافة، تحديث، حذف، تبديل الحالة
- **إدارة الإسناد**: إنشاء، تحديث، حذف، تأكيد
- **الفلترة والبحث**: تطبيق المرشحات والبحث النصي
- **التراجع والإعادة**: استرداد الحالات السابقة

## نظام التقارير

### أنواع التقارير

1. **تقرير المعلم الفردي**
   - معلومات المعلم الأساسية
   - إحصائيات العبء التدريسي
   - تفاصيل المواد والفصول المُسندة
   - مخططات بيانية للعبء

2. **تقرير خطة الإسناد**
   - نظرة عامة على النظام
   - إحصائيات شاملة
   - توزيع العبء على المعلمين
   - تحليل التغطية والمشاكل

3. **التقارير المخصصة**
   - تقارير حسب المستوى الدراسي
   - تقارير حسب التخصص
   - تقارير المشاكل والتحديات

### أدوات التصدير

#### CSV
- تصدير جداول بيانات قابلة للتحرير
- دعم الترميز العربي (UTF-8 with BOM)
- إمكانية استيراد في Excel وأدوات أخرى

#### PDF
- تقارير احترافية للطباعة
- دعم الخطوط العربية
- تخطيط محسن للطباعة
- رؤوس وتذييلات مخصصة

#### HTML
- ملفات مكتفية ذاتياً
- CSS و JavaScript مدمج
- أدوات تفاعلية (بحث، طباعة، مشاركة)
- متوافق مع جميع المتصفحات

## نظام الواتساب والمشاركة

### الميزات

- **رسائل مخصصة**: إنشاء رسائل مخصصة لكل معلم
- **الإرسال الفردي**: إرسال لمعلم واحد
- **الإرسال الجماعي**: إرسال لعدة معلمين
- **تقسيم الرسائل**: تقسيم الرسائل الطويلة تلقائياً
- **المشاركة الأصلية**: استخدام Web Share API
- **النسخ للحافظة**: نسخ الرسائل للحافظة

### أنواع الرسائل

1. **تقرير المعلم**: رسالة شخصية لكل معلم بتفاصيل إسناده
2. **الملخص العام**: إحصائيات شاملة للنظام
3. **تقرير المشاكل**: المشاكل والتحديات التي تحتاج حل

### استخدام الواتساب

```typescript
import { WhatsAppUtils, sendTeacherReport } from './utils/whatsapp';

// إرسال تقرير معلم
sendTeacherReport(state, teacherId, phone, {
  format: 'detailed',
  includeHeader: true,
  includeFooter: true
});

// إنشاء رسالة ملخص
const message = WhatsAppUtils.createSummaryMessage(state, {
  includeTimestamp: true
});

// مشاركة رسالة
await shareMessage('تقرير الإسناد', message);
```

## التصميم والأنماط

### نظام الألوان

- **الأساسية**: درجات الأزرق والرمادي
- **الحالة**: أخضر للنجاح، أصفر للتحذير، أحمر للخطأ
- **العبء**: أخضر (منخفض)، أصفر (متوسط)، أحمر (مرتفع)

### تصميم RTL

- دعم كامل للغة العربية
- تخطيط من اليمين لليسار
- خطوط عربية محسنة
- تصميم متجاوب

### أنماط الطباعة

- تحسين خاص للطباعة
- إزالة العناصر غير المطلوبة
- تقليل استهلاك الحبر
- تخطيط محسن للصفحة

## الاستخدام والتشغيل

### التثبيت والإعداد

1. **التأكد من التبعيات**:
```bash
npm install jspdf react-router-dom
```

2. **إضافة الأنماط**:
```tsx
import './features/assignment/styles/assignment.css';
```

3. **إضافة المسارات**:
```tsx
import { AssignmentPage, TeacherReportView, PlanReportView } from './features/assignment';

// في App.tsx
<Route path="assignment" element={<AssignmentPage />} />
<Route path="assignment/plan" element={<PlanReportView />} />
<Route path="assignment/teacher/:id" element={<TeacherReportView />} />
```

### البيانات الأولية

```typescript
const initialData = {
  teachers: [
    {
      id: '1',
      name: 'أحمد محمد',
      specialization: 'الرياضيات',
      maxLoad: 24,
      currentLoad: 0,
      isActive: true
    }
  ],
  subjects: [
    {
      id: '1',
      name: 'الرياضيات',
      code: 'MATH',
      requiredHours: 6,
      level: 'middle',
      isActive: true
    }
  ],
  classrooms: [
    {
      id: '1',
      name: '1أ',
      grade: 'الأول',
      capacity: 30,
      currentStudents: 28,
      level: 'middle',
      isActive: true
    }
  ]
};
```

## التطوير والمساهمة

### إرشادات التطوير

1. **إتباع معايير TypeScript**: استخدام الأنواع المناسبة
2. **تنسيق الكود**: إتباع معايير Prettier و ESLint
3. **التوثيق**: توثيق الوظائف والمكونات الجديدة
4. **الاختبار**: اختبار الوظائف قبل الإرسال

### إضافة ميزات جديدة

1. **المكونات**: إضافة مكونات في مجلد `components`
2. **الأدوات**: إضافة أدوات في مجلد `utils`
3. **التقارير**: إضافة تقارير في مجلد `reports`
4. **الأنماط**: تحديث ملف `assignment.css`

### الاختبار

```bash
# تشغيل خادم التطوير
npm run dev

# بناء المشروع
npm run build

# اختبار الوظائف
npm test
```

## المشاكل الشائعة والحلول

### مشاكل التصدير

1. **ملفات PDF فارغة**:
   - التأكد من تحميل البيانات
   - فحص إعدادات المتصفح

2. **رسائل الواتساب لا تُرسل**:
   - التأكد من صحة أرقام الهواتف
   - فحص إعدادات المتصفح للنوافذ المنبثقة

3. **مشاكل RTL**:
   - التأكد من تطبيق CSS صحيح
   - فحص إعدادات اللغة

### مشاكل الأداء

1. **بطء في التحميل**:
   - تحسين البيانات المحملة
   - استخدام التحميل الكسول

2. **ذاكرة مرتفعة**:
   - تنظيف البيانات غير المستخدمة
   - تحسين الصور والملفات

## الدعم والمساعدة

### الموارد

- **الوثائق**: في ملف README هذا
- **أمثلة الكود**: في مجلد `examples`
- **اختبارات الوحدة**: في مجلد `tests`

### التواصل

- **المطور**: نظام MOTABEA
- **البريد الإلكتروني**: support@motabea.com
- **الموقع**: https://motabea.com

## رخصة الاستخدام

هذا المشروع محمي بحقوق الطبع والنشر © 2024 MOTABEA. جميع الحقوق محفوظة.

---

**تم إنشاء نظام الإسناد بحب ❤️ من فريق MOTABEA**